/// DTO class that is used for creating Time objects
class TimeListItem {
  String zone;
  String location;
  String flag;

  TimeListItem({required this.zone, required this.location, required this.flag});
}